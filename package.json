{
  "name": "create-bun-react-app",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  },
  "peerDependencies": {
    "typescript": "latest"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "tailwind": "latest"
  },
  "scripts": {
    "dev": "bun ./scripts/helper.ts -w -s",
    "bundle": "NODE_ENV=production bun ./scripts/helper.ts",
    "start": "bun ./scripts/helper.ts -s",
    "lint": "echo 'Add your lint command by editing the package.json file' && exit 1"
  },
  "engines": {
    "bun": ">=1.0.27"
  }
}